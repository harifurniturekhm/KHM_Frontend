# Local development — leave empty (Vite proxy handles /api requests to localhost:5000)
VITE_API_URL=

# For production deployment, set to your Vercel backend URL e.g:
# VITE_API_URL=https://your-backend.vercel.app
